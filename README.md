Reflection Questions

How did you dynamically create and append new elements to the DOM?
Used js inbuilt Classlist function
What steps did you take to ensure accurate updates to the total price?
TO ensure accurate updates to total price, I converted the input to a float and then added or reduced that amount from the totalprice. 
How did you handle invalid input for product name or price? I used NAN function for double checking the price of the of items ensuring that input is a number. I created a function that shows if name input is empty alert. And also if price is under 0. 
What challenges did you face when implementing the remove functionality?
Challenges I faced were just creating the proper formatting in the Class list. And then creating an event listener for that appended delete button. 